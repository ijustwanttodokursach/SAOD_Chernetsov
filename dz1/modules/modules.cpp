#include <iostream>
#include "utils.h"

using namespace std;

int main()
{
    int x = 3, y = 5;
    cout << x + y << endl;
    cout << Plus(x, y) << endl;
    char t[32];
    for (char* pd = t, *ps = str; *pd++ = *ps++;);
}

